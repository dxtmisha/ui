import data from './functions/data'
import image from './functions/image'

export const {
  executeFunction,
  forEach,
  isFilled,
  replaceRecursive,
  toCamelCase,
  toKebabCase
} = data

export const {
  createImage,
  getFileResult,
  getSRC,
  isImage
} = image
